{
  "name": "ayden-proto",
  "private": true,
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
